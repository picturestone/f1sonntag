{% extends 'adminbase.html.twig' %}

{% block body %}
<div class="container-xl">
    <h1>Passwort ändern für {{ user.firstName }} {{ user.lastName }}</h1>

    <div>
        {{ form(form, { method: 'POST' }) }}
    </div>
</div>
{% endblock %}
