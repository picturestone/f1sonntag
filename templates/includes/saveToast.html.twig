{% if isVisible %}
    <div class="toast show align-items-center position-absolute m-3 top-0 end-0 bg-white" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
            <div class="toast-body d-flex">
                <div style="height: 100%; width: 8px;" class="rounded me-2 bg-success"></div>

                Speichern erfolgreich.
            </div>
            <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
{% endif %}
